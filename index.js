// File that handles interactions between user and atm

const accountInfo = require('./account');

const balanceCheck = prompter("Would you like to check your account balance?");
function getBalance(input){
    if(input === "yes"){
        alert(`Your current balance is ${acctBal}`);
    }else{
        
    }

}